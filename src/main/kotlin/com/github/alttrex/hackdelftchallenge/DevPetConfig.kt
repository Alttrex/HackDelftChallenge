package com.github.alttrex.hackdelftchallenge

/**
 * Central configuration for DevPet plugin.
 * Set [DEV_MODE] to false before release to hide the development panel.
 */
object DevPetConfig {
    /** When true, a "Dev" tab is shown in the tool window for debugging pet state. */
    const val DEV_MODE: Boolean = true
}
